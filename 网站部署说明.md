# 预制菜名单展示站部署说明

## 本地查看

```powershell
cd C:\Users\sky\Desktop\预制菜追杀\docs
python -m http.server 8000
```

然后打开：

```text
http://127.0.0.1:8000/
```

## 更新数据

从飞书多维表格导出 Excel 后，覆盖根目录的 `预制菜.xlsx`，再执行：

```powershell
python .\scripts\export_prepared_foods.py
```

生成的数据会写入：

- `docs/data/prepared-foods.json`
- `docs/data/prepared-foods.csv`

## GitHub Pages 发布

1. 在 GitHub 创建一个新仓库，建议命名为 `prepared-food-list`。
2. 在本地执行：

```powershell
git init
git add docs scripts .github .gitignore 网站部署说明.md
git commit -m "init prepared food list site"
git branch -M main
git remote add origin https://github.com/xinyueguang/prepared-food-list.git
git push -u origin main
```

3. 打开仓库的 `Settings -> Pages`。
4. `Build and deployment` 选择 `Deploy from a branch`。
5. 分支选择 `main`，目录选择 `/docs`。
6. 保存后等待 GitHub Pages 部署完成。

网站地址通常是：

```text
https://xinyueguang.github.io/prepared-food-list/
```

## 投稿入口

当前网站的投稿按钮会打开 GitHub Issues 预填页面。仓库名如果不是 `prepared-food-list`，请修改 `docs/app.js` 里的：

```js
githubRepo: "prepared-food-list"
```

如果以后有真实后端接口，可以把 `docs/app.js` 里的 `submissionEndpoint` 改成你的接口地址，网站会自动改为 `POST JSON` 投稿。
